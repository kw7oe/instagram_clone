<%= form_for(post.comments.build, remote: true) do |f| %>
	<%= hidden_field_tag :user_id, current_user.id %>
	<%= hidden_field_tag :post_id, post.id %>
  <div class="field">
    <%= f.text_field :comment, class: "comment_input", placeholder: "Add a comment..." %>
  </div>
<% end %>